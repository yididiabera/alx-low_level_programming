#include <stdio.h>
#include "cfuns.h"
/**
 * add - the function to add two integers
 *@a: integer passed to the function.
 *@b: integer passed to the function.
 * Return: returns the sum of two integers.
 */
int add(int a, int b)
{
	return (a + b);
}
