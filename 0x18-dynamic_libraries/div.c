#include <stdio.h>
#include "cfuns.h"
/**
 * div - it divides the dividened by the divisor.
 *@a: integer passed to the function.
 *@b: integer passed to the function.
 * Return: returns the quationt of the two integers.
 */
int div(int a, int b)
{
	return (a / b);
}
