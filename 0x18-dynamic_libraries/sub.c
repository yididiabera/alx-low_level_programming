#include <stdio.h>
#include "cfuns.h"
/**
 * sub - the function to substract one int from another.
 *@a: integers passed to the function.
 *@b: integers passed to the function.
 * Return: returns difference.
 */
int sub(int a, int b)
{
	return (a - b);
}
